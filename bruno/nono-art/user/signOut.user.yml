info:
  name: signOut.user
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{baseURL}}/api/user/sign-out'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
