info:
  name: signIn.user
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/user/sign-in"
  body:
    type: json
    data: |-
      {
        "login": "qwe",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
